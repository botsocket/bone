/**
 * Throws an error if a condition is falsy where:
 *
 * @param condition - The condition to evaluate.
 * @param message - The error message to throw.
 */
declare function assert(condition: unknown, message?: unknown): void;

export = assert;
